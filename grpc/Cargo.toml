[package]
name = "dag_grpc"
version = "0.1.0"
edition = "2024"

[package.metadata.cargo-machete]
ignored = ["prost"]

[dependencies]
prost = { workspace = true }
tonic = { workspace = true }

[build-dependencies]
tonic-build = "0.13.1"
